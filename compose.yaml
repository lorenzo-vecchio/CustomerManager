version: '3.8'

services:
  mysql:
    image: "mysql:latest"
    environment:
      MYSQL_ROOT_PASSWORD: ${CM_MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${CM_MYSQL_DATABASE}
      MYSQL_USER: ${CM_MYSQL_USERNAME}
      MYSQL_PASSWORD: ${CM_MYSQL_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - "mysql_data:/var/lib/mysql"

  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"
    volumes:
      - "redis_data:/data"

volumes:
  mysql_data:
  redis_data:
